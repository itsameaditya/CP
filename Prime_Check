bool prime(int n)
{
  if(n<2) return false;
  if(n<=3) return true;
  
  if(!(n%2) || !(n%3)) return false;
  for(int i = 6 ; i*i <=n ; i+=6)
    if(!(n%1) || !(n%(i+2)) return false;
    
  return true;
 }
 
